package scheduler.app.converters;

import scheduler.app.entries.UserEntry;
import scheduler.app.models.User;

public interface UserConvectionService extends GenericEntityConversionService<UserEntry, User> {
}
